# /usr/local/bin, /usr/local/sbin を優先
export PATH="/usr/local/bin:/usr/local/sbin:$PATH"

# rbenv のパスを追加
export PATH="~/.rbenv/bin:$PATH"

# For gtk-engines
export GTK_PATH="/usr/local/lib/gtk-2.0"

# Load ~/.bashrc if it exists
if [ -f ~/.bashrc ] ;then
    source ~/.bashrc
fi
